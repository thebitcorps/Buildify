.navbar.navbar-default.navbar-static-top
  .container
    %button.navbar-toggle.collapsed{ "aria-expanded" => "false", type: "button", data: { target: "#main-navbar", toggle: "collapse" } }
      %span.icon-bar
      %span.icon-bar
      %span.icon-bar
    = link_to 'Allpino', root_path, class: 'navbar-brand'
    #main-navbar.collapse.navbar-collapse
      %ul.nav.navbar-nav
        %li= link_to 'Inicio', root_path
        = render partial: "common/menubars/#{current_user.roles.first.to_s}"
      %ul{class: 'nav navbar-nav navbar-right'}
        %li.dropdown
          %a.dropdown-toggle{'aria-expanded' => 'false', 'aria-haspopup' => 'true', 'data-toggle' => 'dropdown', href: '#', role: 'button'}
            %span{class: 'fa fa-bell-o'}
            %span{class: 'label label-default'}
              = current_user.notifications.unseen.count
            %span.caret
          %ul.dropdown-menu
            %li
              %a
                ='No hay notificaciones nuevas' if current_user.notifications.unseen.empty?
            - current_user.notifications.unseen.each do |notification|
              %li= link_to render_activity(notification.activity),seen_notification_path(notification),method: :post
        %li
          = link_to current_user, data: { 'toggle' => 'tooltip' ,'placement' => 'bottom' }, 'title' => t('profile') do
            %span{class: 'fa fa-user'}
        %li
          = link_to destroy_user_session_path, method: :delete, data: { 'toggle' => 'tooltip' ,'placement' => 'bottom' },'title' => t('sign_out') do
            %span{class: 'fa fa-sign-out'}
