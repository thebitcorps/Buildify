%p#notice= notice

.page-header
  .row
    .col-md-6
      %h1
        = @construction.title
        %small= @construction.resident.name
    .col-md-6
      %h3.text-center
        %small= "Desde " +  spanish_formated_date(@construction.start_date, false)
      .progress
        - if @construction.available_days > @construction.days_passed
          .progress-bar.progress-bar-info{"aria-valuemax" => "100", "aria-valuemin" => "0", "aria-valuenow" => "#{}", :role => "progressbar", :style => "width: #{persentage_of_number(@construction.days_passed, @construction.available_days)}%;"}
            = [@construction.days_passed, @construction.available_days].join("/") + " Dias"
        - else
          .progress-bar.progress-bar-warning{"aria-valuemax" => "100", "aria-valuemin" => "0", "aria-valuenow" => "#{}", :role => "progressbar", :style => "width: 100%;"}
            = [@construction.days_passed, @construction.available_days].join("/") + " Dias"
      %h3.text-center.construction-special-date
        %small= "Termina " +  spanish_formated_date(@construction.finish_date, false)

= link_to 'Nueva requisición', new_requisition_path(construction: @construction.id), class: 'btn btn-primary'
= link_to 'Editar', edit_construction_path(@construction), class: 'btn btn-default'
= link_to 'Regresar', constructions_path, class: 'btn btn-default'
