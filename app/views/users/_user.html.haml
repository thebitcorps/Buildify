%tr
  %td=link_to user.name, user_path(user)
  %td= user.phone
  %td= user.email
  %td= user.role
  %td= link_to 'Edit', edit_user_path(user)
  %td= link_to 'Destroy', user_path(role: params[:role],id: user.id), :method => :delete, :data => { :confirm => 'Are you sure?' }
