%tr
  %td=link_to user.name, user_path(user)
  %td= user.phone
  %td= user.email
  %td
    .btn-group
      = link_to 'Editar', edit_user_path(user), class: 'btn btn-warning'
      = link_to 'Borrar', user_path(user), class: 'btn btn-danger', method: :delete, data: { :confirm => '¿Estás seguro?' }
