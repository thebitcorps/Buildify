= link_to purchase_order, class: "list-group-item construction-special-row" do
  .row
    .col-md-12
      %h4.pull-right
        %small
          = purchase_order.completed? ? "Enviada a proveedor" : "Pendiente"
      %table
        %tr
          %td
            %h4
              %span{class: "label label-#{purchase_order.get_color}"}
                = purchase_order.formated_folio
          %td
            %h4
              %small
                = item_materials_status_message purchase_order.item_materials_status_count
          %td
            %h4
              %span{class: "label label-info"}
                = purchase_order.stamp.blank? ? "Sin firmar" : "Firmada"
